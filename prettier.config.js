/**
 * @type {import("prettier").Config}
 */
const config = {
    semi: true,
    tabWidth: 4,
    singleQuote: false,
    jsxSingleQuote: false,
    trailingComma: "all",
    quoteProps: "consistent",
    printWidth: 80,
};

export default config;
