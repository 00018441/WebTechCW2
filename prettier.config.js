/**
 * @type {import("prettier").Config}
 */
const config = {
    semi: true,
    tabWidth: 4,
    singleQuote: false,
    jsxSingleQuote: false,
    trailingComma: "all",
    quoteProps: "consistent",
    printWidth: 120,
};

export default config;
